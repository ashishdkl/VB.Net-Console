Module Module1

    Sub Main()
        'adding string and integer
        Dim i As Integer = 10
        Dim b As String = "ashish"
        Dim result As String
        result = i & b ' we can also write result=i+b if there is same data type s
        'dim i as string="ashish"
        'dim b as string="dhakal"
        Console.WriteLine(result)
        Console.ReadLine()


    End Sub

End Module
